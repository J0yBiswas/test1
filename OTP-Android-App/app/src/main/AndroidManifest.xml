<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Minimal footprint on purpose: only RECEIVE_SMS (fires the manifest receiver
         on the incoming OTP, even when the app is closed), INTERNET (push the code to
         the fixed relay), and POST_NOTIFICATIONS (the "OTP forwarded" toast on
         Android 13+). We do NOT request READ_SMS, so the app never touches your
         existing inbox — which keeps it clear of Play Protect's "reads messages" flag. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.IvacOtp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.IvacOtp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fires on every inbound SMS. The receiver decides whether it is an IVAC
             OTP and, if so, forwards it. We never abort the broadcast. -->
        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
