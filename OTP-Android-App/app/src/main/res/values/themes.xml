<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- A Material3 splash/base theme used before Compose takes over. -->
    <style name="Theme.IvacOtp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/ivac_bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/ivac_bg</item>
    </style>
</resources>
